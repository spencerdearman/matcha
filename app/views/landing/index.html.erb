<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Matcha Feed</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
</head>
<body>
  <div class="feed-container">
    <div class="feed-container-inner">
      <% if @posts.any? %>
        <% @posts.each do |post| %>
          <div class="post">
            <div class="post-header">
              <div class="profile-info">
                <!-- Conditional Link for Username -->
                <% if post.user == current_user %>
                  <!-- If it's the current user, link to their profile -->
                  <a href="<%= profile_path %>">
                    <%= image_tag post.user.image.attached? ? post.user.image : "default-profile.png", alt: "Profile Picture", class: "profile-pic" %>
                    <h3><%= post.user.username.capitalize %></h3>
                  </a>
                <% else %>
                  <!-- If it's another user, link to that user's profile -->
                  <a href="<%= user_path(post.user) %>">
                    <%= image_tag post.user.image.attached? ? post.user.image : "default-profile.png", alt: "Profile Picture", class: "profile-pic" %>
                    <h3><%= post.user.username.capitalize %></h3>
                  </a>
                <% end %>
              </div>
            </div>

            <div class="post-body">
              <% if post.image.attached? %>
                <%= image_tag post.image, alt: "Post Image", class: "post-image" %>
              <% end %>
              <p class="post-caption"><%= post.caption %></p>
            </div>
          </div>
        <% end %>
      <% else %>
        <p>No posts available.</p>
      <% end %>
    </div>
  </div>

  <style>
    /* Overall Feed Container */
    .feed-container {
      display: flex;
      justify-content: center;
      padding: 20px;
      max-width: 100%;
      margin: 0 auto;
      height: auto;
      flex-direction: column;
    }

    /* This inner container will hold all posts */
    .feed-container-inner {
      display: flex;
      flex-direction: column;
      gap: 20px;
      justify-content: flex-start;
      width: 100%;
      margin-top: 20px;
    }

    /* Individual Post Container */
    .post {
      width: 100%;
      max-width: 350px;
      border: 1px solid #ccc;
      border-radius: 10px;
      background-color: white;
      padding: 15px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      display: flex;
      flex-direction: column;
      justify-content: flex-start;
      margin: 0 auto;
      height: auto;
    }

    /* Post Body (image and caption) */
    .post-body {
      margin-top: 10px;
      text-align: center;
    }

    .post-image {
      width: 100%;
      height: 400px;
      object-fit: cover;
      border-radius: 8px;
      margin-bottom: 10px;
    }

    /* Post Header (username and profile image) */
    .post-header {
      display: flex;
      align-items: center;
      margin-bottom: 10px;
    }

    .profile-info {
      display: flex;
      align-items: center;
      justify-content: flex-start;
    }

    .profile-info a {
      display: flex;
      align-items: center;
      text-decoration: none; /* Removes the underline from the link */
    }

    .profile-pic {
      width: 50px;
      height: 50px;
      border-radius: 50%;
      object-fit: cover;
      margin-right: 10px;
    }

    .post-header h3 {
      font-size: 16px;
      font-weight: bold;
    }

    .post-caption {
      font-size: 14px;
      color: #333;
    }

    /* Button for navigation */
    .btn-edit-profile {
      display: inline-block;
      padding: 10px 20px;
      background-color: #4CAF50;
      color: white;
      border-radius: 5px;
      text-decoration: none;
      transition: background-color 0.3s ease;
      margin-top: 20px;
    }

    .btn-edit-profile:hover {
      background-color: #45a049;
    }

    /* Footer Fix */
    footer {
      text-align: center;
      padding: 10px;
      background-color: #333;
      color: white;
      margin-top: 30px;
      position: relative;
      bottom: 0;
      width: 100%;
    }

    /* Ensuring the whole body scrolls, not just a part of it */
    body {
      height: 100vh;
      overflow-y: auto; /* Allow the entire page to scroll */
      margin: 0;
    }
  </style>
</body>
</html>
