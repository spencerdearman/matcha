<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Welcome to Matcha</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
</head>
<body>
  <!-- Profile Header with Vertical Frame Layout -->
  <div class="profile-header">
    <!-- Profile Image -->
    <div class="profile-image">
      <% if @user.image.attached? %>
        <%= image_tag @user.image, alt: "Profile Picture", class: "profile-pic" %>
      <% else %>
        <%= image_tag "default-profile.png", alt: "Default Profile", class: "profile-pic" %>
      <% end %>
    </div>

    <!-- Profile Info (Username, Bio) -->
    <div class="profile-info">
      <div class="header">
        <!-- Username -->
        <h2><%= @user.username.capitalize %></h2>
      </div>

      <!-- Bio Section -->
      <div class="bio">
        <p><%= @user.bio %></p>
      </div>
    </div>
  </div>

  <!-- User's Posts Section -->
  <h2 class="your-posts-heading"><%= @user.username.capitalize %>'s Posts</h2>

  <% if @user.posts.any? %>
    <div class="profile-posts">
      <% @user.posts.each do |post| %>
        <div class="post">
          <%= image_tag(post.image, alt: 'Post Image', class: 'post-image') if post.image.attached? %>
          <p><%= post.caption %></p>
        </div>
      <% end %>
    </div>
  <% else %>
    <p>You haven't created any posts yet.</p>
  <% end %>

  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #f4f4f4;
    }

    /* Profile Header Section */
    .profile-header {
      display: flex;
      justify-content: center;  /* Center the profile section */
      align-items: center;
      padding: 40px;
      background-color: #fff;
      border-bottom: 1px solid #ccc;
      width: 100%;
    }

    /* Profile Image Styling */
    .profile-image {
      width: 150px;
      height: 150px;
      border-radius: 50%;
      overflow: hidden;
      margin-right: 20px;
    }

    .profile-pic {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    /* Profile Info Styling (Username, Bio) */
    .profile-info {
      display: flex;
      flex-direction: column;
      justify-content: flex-start;
      align-items: center;  /* Center the content in the profile info */
      width: 300px;
    }

    .header {
      display: flex;
      justify-content: center;
      width: 100%;
    }

    .profile-info h2 {
      font-size: 2em;
      margin-bottom: 10px;
    }

    /* Bio Styling */
    .bio p {
      font-size: 1.2em;
      color: #555;
      text-align: center;  /* Center the bio text */
    }

    /* Your Posts Section */
    .your-posts-heading {
      text-align: center;
      padding: 20px 0;
      font-size: 2.5em;
      font-weight: bold;
      margin-top: 20px;
    }

    .profile-posts {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
      gap: 20px;
      padding: 0 20px;
    }

    .post {
      display: flex;
      flex-direction: column;
      align-items: center;
      border: 1px solid #ccc;
      padding: 10px;
      background-color: #fff;
      border-radius: 8px;
      text-align: center;
    }

    .post-image {
      width: 100%;
      height: 300px;
      object-fit: cover;
      border-radius: 8px;
      margin-bottom: 10px;
    }

    footer {
      text-align: center;
      padding: 10px;
      background-color: #333;
      color: white;
      margin-top: 30px;
      position: relative;
      bottom: 0;
      width: 100%;
    }
  </style>
</body>
</html>
