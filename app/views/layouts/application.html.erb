<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Welcome to Matcha</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <style>
    /* Body and overall layout */
    body {
      display: flex;
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #f5f2e9;
      height: 100vh;
    }

    /* Sidebar */
    .sidebar {
      position: fixed;
      left: 0;
      top: 0;
      width: 200px;
      height: 100%;
      background-color: #4b4b4b;
      padding-top: 30px;
      color: #f5f2e9;
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    /* Sidebar links */
    .sidebar a {
      color: #f5f2e9;
      text-decoration: none;
      margin: 10px 0;
      font-size: 18px;
    }

    /* Sidebar title */
    .sidebar h1 {
      font-size: 35px;  /* Increase font size for "MATCHA" */
      font-weight: bold;
      margin-bottom: 30px;  /* Add space below the title */
    }

    /* Ensure the link does not override font size */
    .sidebar h1 a {
      color: #4b4b4b;
      text-decoration: none;
      font-size: 35px;
    }

    /* Logout button */
    .logout-button {
      position: absolute;
      bottom: 50px; /* Place slightly above the bottom */
      left: 10px;   /* Align with the sidebar padding */
      background-color: #558255;
      color: white;
      font-size: 16px;
      padding: 10px 30px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      width: calc(100% - 40%); /* Fit within the sidebar width */
      text-align: center;
    }

    .logout-button:hover {
      background-color: #476e47;
    }

    /* Header for content section */
    header {
      background-color: #333;
      padding: 10px;
      color: #f5f2e9;
      position: fixed;
      width: calc(100% - 200px);
      left: 200px;
      top: 0;
      z-index: 10;
    }

    header h1 {
      margin: 0;
      font-size: 24px;
    }

    /* Content section */
    .content {
      margin-left: 200px;
      padding: 20px;
      padding-top: 80px; /* Add some padding to avoid overlap with the header */
      width: calc(100% - 200px);
      overflow-y: scroll;
      height: 100%;
    }

    /* Feed container */
    .feed-container {
      display: flex;
      flex-direction: column;
      gap: 20px;
    }

    /* Individual Post Container */
    .post {
      width: 100%;
      max-width: 350px;
      border: 1px solid #ccc;
      border-radius: 10px;
      background-color: #f5f2e9;
      padding: 15px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      margin: 0 auto;
      height: auto;
      display: flex;
      flex-direction: column;
      justify-content: flex-start;
    }

    .post-body {
      margin-top: 10px;
      text-align: center;
    }

    .post-image {
      width: 100%;
      height: 400px;
      object-fit: cover;
      border-radius: 8px;
      margin-bottom: 10px;
    }

    .profile-info {
      display: flex;
      align-items: center;
      justify-content: flex-start;
    }

    .profile-pic {
      width: 50px;
      height: 50px;
      border-radius: 50%;
      object-fit: cover;
      margin-right: 10px;
    }

    .post-header h3 {
      font-size: 16px;
      font-weight: bold;
    }
  </style>
</head>
<body>

  <!-- Sidebar -->
  <div class="sidebar">
    <h1><a href="<%= root_path %>" style="color: white; text-decoration: none;">MATCHA</a></h1>
    <% if user_signed_in? %>
      <a href="<%= root_path %>">Feed</a>
      <a href="<%= profile_path %>">Profile</a>
    <% else %>
      <a href="<%= root_path %>">Home</a>
      <a href="<%= new_user_session_path %>">Login</a>
      <a href="<%= new_user_registration_path %>">Sign Up</a>
    <% end %>

    <% if user_signed_in? %>
  <!--    <a href="<%#= destroy_user_session_path %>" method="delete">Log out</a>-->
      <%#= button_to "Sign out", destroy_user_session_path, method: :delete, class: "logout-button" %>
      <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "logout-button" %>
    <% end %>
  </div>

  <!-- Main Content -->
  <div class="content">
    <%= yield %> <!-- Main content like the feed will be rendered here -->
  </div>

</body>
</html>
